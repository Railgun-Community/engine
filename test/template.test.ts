/* globals describe it */
import * as chai from 'chai';

const { expect } = chai;

describe('Test Template', () => {
  it('math should work correctly', () => {
    expect(3 + 4).to.equal(7);
  });
});
